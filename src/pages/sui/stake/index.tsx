import { Flex } from "components/base/container";
import { Box } from "components/base";

const SuiStake = () => {
  return (
    <iframe
      src="https://staking.staked.us/sui-staking"
      width="100%"
      seamless
      style={{ border: "none" }}
    />
  );
};

export default SuiStake;
